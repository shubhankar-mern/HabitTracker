<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=M+PLUS+2:wght@200&family=Open+Sans+Condensed:wght@300&family=Poppins:ital,wght@0,100;1,200&family=Roboto+Condensed&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="./static/css/home.css">
    <script src="https://use.fontawesome.com/releases/v5.15.4/css/all.css" integrity="sha384-DyZ88mC6Up2uqS4h/KRgHuoeGwBcD4Ng9SiP4dIRy0EXTlnuz47vAwmeGwVChigm" crossorigin="anonymous"></script></script>
</head>
<body>
    <div class="container">
        <!-- Heading -->
        <div class="heading">
            <span>Habit Tracker|Built with luv</span>
            <img src="./static/images/habitTrackerLogo.png">
        </div>
        <!-- Input Section-->
        <form action="/addHabit" method="POST">
            <div class="takeInput">
         <input id="habitInput" type="text" name="habitName" placeholder="Add habit?" required>
         <input id="timeInput" type="time" name="time" required>
         <button id="btn" type="submit">
         Add
         </button>
        </div>
        </form>
       <!--habits are displayed haerae-->
    <ul>
       <% if(allHabits.length==0){%>
        <li class="warning">
            No habit present in bucket!!!
        </li>
        <% }else{ %>
            <% for(let i of allHabits) {%>
                <li class="habitList">
             
                  <a class="habit-name" href="/details/?id=<%= i.id %>">
                      <span><%= i.name %></span>
                  </a>
                  <span class="time"><%= i.time %></span>
                  <a class="del" href="/delete/?id=<%=i.id%>">
                      <button>Delete</button>               
                  </a>
                </li>
                <% } %>
                <% } %>
    </ul>
    </div>
    
</body>
</html>